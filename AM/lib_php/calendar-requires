Need to add 2 columns in sips_sd_reservations_types:
    ALTER TABLE `sips_sd_reservations_types` ADD `min_time` INT NOT NULL , ADD `max_time` INT NOT NULL ;
    ALTER TABLE `sips_sd_reservations` ADD `changed` INT(11) NOT NULL DEFAULT '0' AFTER `lead_id`;
